# @spcookie/erii-browser-driver-darwin-arm64

Erii browser driver — macOS Apple Silicon native libraries.

This package is an optional dependency of `@spcookie/erii`. It is automatically installed on macOS ARM64 (Apple Silicon)
machines.
