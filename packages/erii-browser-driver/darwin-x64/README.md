# @spcookie/erii-browser-driver-darwin-x64

Erii browser driver — macOS Intel native libraries.

This package is an optional dependency of `@spcookie/erii`. It is automatically installed on macOS x64 (Intel) machines.
