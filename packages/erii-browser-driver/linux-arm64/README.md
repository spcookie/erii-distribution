# @spcookie/erii-browser-driver-linux-arm64

Erii browser driver — Linux ARM64 native libraries.

This package is an optional dependency of `@spcookie/erii`. It is automatically installed on Linux ARM64 machines.
