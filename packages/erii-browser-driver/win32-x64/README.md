# @spcookie/erii-browser-driver-win32-x64

Erii browser driver — Windows x64 native libraries.

This package is an optional dependency of `@spcookie/erii`. It is automatically installed on Windows x64 machines.
