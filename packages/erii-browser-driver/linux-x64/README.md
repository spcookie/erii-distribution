# @spcookie/erii-browser-driver-linux-x64

Erii browser driver — Linux x64 native libraries.

This package is an optional dependency of `@spcookie/erii`. It is automatically installed on Linux x64 machines.
